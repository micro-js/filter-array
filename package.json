{
  "name": "@f/filter-array",
  "description": "Functional, data-last array filter. Should also be faster than the native Array.prototype.filter",
  "repository": "git://github.com/micro-js/filter-array.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
